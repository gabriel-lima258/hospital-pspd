/**
 * Camada de domínio (hexagonal) — regras de anonimização/agregação e mapeamento FHIR puras,
 * SEM framework (testáveis isoladamente). (Scaffold D2: ainda vazia.)
 */
package com.hospital.datatransform.domain;
