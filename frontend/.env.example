# Exemplo de configuração do frontend. Copie para .env (real) ou .env.local (override local).
# Modo REAL: preencha as 4 URLs/IDs abaixo e deixe VITE_DEMO_MODE=false.
VITE_API_GATEWAY_URL=http://localhost:9000
VITE_KEYCLOAK_URL=http://localhost:8080
VITE_KEYCLOAK_REALM=hospital
VITE_KEYCLOAK_CLIENT_ID=hospital-frontend
VITE_DEMO_MODE=false
