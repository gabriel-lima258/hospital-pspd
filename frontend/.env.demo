# Modo DEMO (vitrine offline, sem backend/Keycloak). Uso: copie para .env.local
#   cp frontend/.env.demo frontend/.env.local   &&   npm run dev
# Sem VITE_KEYCLOAK_URL o config.ts liga IS_DEMO=true (dados mockados, login falso).
VITE_DEMO_MODE=true
